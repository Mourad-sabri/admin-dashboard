import styled from "styled-components"

export const S_CardMap = styled.div`
  width: 600px;
  height: 390px;
  overflow: hidden;
  border-radius: ${(p) => p.theme.rounded.sm};
  background-color: var(--bg-primary);
`
