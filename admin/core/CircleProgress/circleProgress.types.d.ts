export interface CircleProgressProps {
  percent?: number // pass percent to show in the progess bar
  size?: number // size of the progress bar
  countUp?: boolean /**@default true */ // apply count animation
}
